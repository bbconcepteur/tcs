(function($) { 
	$('document').ready(function(){
		//alert(ss.i18n.getLocale());
		
		//$("ul.tabs").tabs("div.panes > div",{initialIndex:selected_tab}); // setup ul.tabs to work as tabs for each div directly under div.panes
		
		
		//$( "#link_banner" ).imageScroller( {loading:'Wait please...'} );
	});
})(jQuery);
